# Tools

Future home for adapters around hosted tools, MCP tools, and local CLIs.

Adapters must keep read-only operations separate from write operations and expose predictable JSON or concise text output.

