# Telemetry

Future home for trace schemas, run logs, tool call records, guardrail events, and eval score snapshots.

Current deterministic evidence lives in `evals/results/`, which is intentionally gitignored.

