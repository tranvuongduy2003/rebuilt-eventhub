interface:
  display_name: "shadcn"
  short_description: "Use shadcn UI registries and components"
  default_prompt: "Use $shadcn to find and add the right UI components for this screen."

dependencies:
  tools:
    - type: "mcp"
      value: "shadcn"
      description: "shadcn MCP server"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
